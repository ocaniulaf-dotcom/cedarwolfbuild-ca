---
const {
  name,
  description,
  url,
  image = "https://cedarwolfbuild.ca/images/CEDARWOLF_BUILD_DESIGN_LOGO.png",
  serviceType = name,
  serviceOutput = "Completed construction or renovation service",
  areaServed = ["Kelowna", "West Kelowna", "Lake Country", "Peachland", "Vernon", "Okanagan"],
} = Astro.props;

const siteUrl = "https://cedarwolfbuild.ca";
const serviceUrl = url.startsWith("http") ? url : new URL(url, siteUrl).toString();
const imageUrl = image.startsWith("http") ? image : new URL(image, siteUrl).toString();

const schema = {
  "@context": "https://schema.org",
  "@type": "Service",
  "@id": `${serviceUrl}#service`,
  name,
  serviceType,
  description,
  url: serviceUrl,
  image: imageUrl,
  serviceOutput,
  providerMobility: "dynamic",
  provider: {
    "@type": "HomeAndConstructionBusiness",
    "@id": "https://cedarwolfbuild.ca/#business",
    name: "CedarWolf Build + Design",
    url: "https://cedarwolfbuild.ca/",
    telephone: "+1-778-594-1722",
    email: "projects@cedarwolfbuild.ca",
    brand: {
      "@type": "Brand",
      name: "CedarWolf Build + Design"
    }
  },
  areaServed: areaServed.map((area) => ({
    "@type": "Place",
    name: area
  })),
  offers: {
    "@type": "Offer",
    availability: "https://schema.org/InStock",
    priceCurrency: "CAD",
    url: serviceUrl,
    eligibleRegion: {
      "@type": "AdministrativeArea",
      name: "Okanagan, British Columbia"
    }
  },
  availableChannel: {
    "@type": "ServiceChannel",
    serviceUrl: "https://cedarwolfbuild.ca/contact/",
    availableLanguage: ["English", "Spanish"]
  },
  termsOfService: "https://cedarwolfbuild.ca/contact/"
};

const schemaJson = JSON.stringify(schema);
---

<script type="application/ld+json" set:html={schemaJson} />
